{$APP_DOMAIN} {
    encode zstd gzip

    header {
        X-Content-Type-Options nosniff
        Referrer-Policy no-referrer
        -Server
    }

    reverse_proxy epiapp:3000 {
        header_up X-Real-IP {remote_host}
    }
}
