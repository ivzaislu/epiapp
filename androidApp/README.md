# EpiApp Android APK

Ветка `main` содержит универсальный Android companion для self-hosted EpiApp.

APK **не привязан к конкретному домену**. При первом запуске пользователь вводит HTTPS-origin своего EpiApp-сервера и одноразовый код из Telegram-бота этого сервера.

Основной интерфейс остаётся единым WebView-интерфейсом сервера; нативная Android-часть отвечает за device credentials, локальные alarms, full-screen alarm, звук/вибрацию, восстановление после reboot, background schedule sync и безопасные обновления из GitHub Releases.

## Быстрое подключение

1. Telegram-пользователь уже должен быть `admin`, `parent` или `child` на своём EpiApp.
2. В боте нажать `📲 Подключить Android`.
3. Бот покажет, например:

```text
Сервер:
https://epiapp.example.com

Одноразовый код:
123 456
```

4. Ввести оба значения в APK.

Код действует 5 минут и используется один раз. После pairing сервер хранит только hash случайного device-token; raw token шифруется на Android через Keystore.

## Роли

- `child` — детский UI + native alarms;
- `parent` — родительский UI, статистика и настройки;
- `admin` — родительский/admin UI.

## Сборка debug

```bash
gradle --no-daemon :androidApp:assembleDebug
```

APK:

```text
androidApp/build/outputs/apk/debug/androidApp-debug.apk
```

CI ветки `main` выполняет эту сборку автоматически.

Для стабильного release APK используйте один приватный signing key: [../docs/RELEASE.md](../docs/RELEASE.md).

Полная документация:

- [../docs/ANDROID.md](../docs/ANDROID.md)
- [../docs/INSTALL.md](../docs/INSTALL.md)
- [../docs/SECURITY.md](../docs/SECURITY.md)
- [../docs/TROUBLESHOOTING.md](../docs/TROUBLESHOOTING.md)

EpiApp не назначает лечение и не подтверждает фактический приём лекарства. Выключение alarm не создаёт отметку о приёме.


## Онлайн-обновления

Release APK проверяет `https://api.github.com/repos/ivzaislu/epiapp/releases/latest` не чаще одного раза в 6 часов. Если опубликован более новый `versionCode`, приложение предлагает скачать официальный APK.

Перед запуском системного установщика EpiApp проверяет:

- `update.json` из того же GitHub Release;
- SHA-256 APK;
- package name `org.epiapp.android`;
- увеличенный `versionCode`;
- совпадение signing certificate с уже установленным EpiApp.

На Android 8+ при первом таком обновлении система может один раз попросить разрешить EpiApp установку из этого источника. Само обновление всё равно подтверждается пользователем в системном установщике Android.


## Переход к нативным уведомлениям — ветка 0.4.1.1

Начиная с экспериментальной ветки `0.4.1.1`, Android-устройство родителя/администратора дублирует ключевые Telegram-сигналы:

- когда сервер подтверждает новую отметку ребёнка, Android показывает нативное уведомление о принятом утреннем/вечернем приёме;
- на стадиях опоздания, совпадающих с Telegram-напоминаниями, Android делает фоновую проверку сервера и показывает уведомление только если отметки действительно нет;
- Telegram пока остаётся параллельным резервным каналом;
- родительский Android синхронизируется с сервером не реже чем примерно раз в 15 минут, а проверки опоздания планируются отдельными AlarmManager-событиями.

Для безопасности APK не показывает уведомление «не принято» по одному только локальному таймеру: сначала требуется подтверждение актуального состояния от self-hosted EpiApp-сервера. Если телефон родителя офлайн, Telegram остаётся основным резервным каналом, а APK догонит состояние при следующей синхронизации.

Это первый этап перехода. Для действительно мгновенной доставки событий в закрытое приложение следующим этапом нужен отдельный push-транспорт; до его внедрения мы не отключаем Telegram.


### Надёжность напоминаний ребёнку в 0.4.1.1

Ветка 0.4.1.1 исправляет нативные напоминания ребёнку:

- добавлено уведомление непосредственно в заданное время приёма;
- повторные стадии опоздания сохраняются: обычная и срочные;
- новые notification channel ID принудительно отделяют эту версию от старых/случайно бесшумных каналов Android;
- обычный канал использует высокий приоритет, системный notification sound и вибрацию;
- срочный канал использует alarm sound, вибрацию и full-screen alarm, когда Android разрешает;
- если фоновая синхронизация совпала по времени с alarm, прошедшая не более чем на 10 минут стадия догоняется немедленно, а не переносится на следующий день;
- уже показанная стадия запоминается на текущую дату, чтобы синхронизации не создавали повторные дубли;
- при отказе в Android 13+ notification permission ребёнку показывается понятный переход в системные настройки уведомлений.


### Персональный тихий режим родителя

В родительском кабинете Android 0.4.1.1 появляется настройка **«Громкий звук для срочных предупреждений»**. Она хранится локально на конкретном Android-устройстве и не влияет на второго родителя.

По умолчанию срочное предупреждение использует отдельный alarm-канал со звуком и вибрацией. Если выключить переключатель, срочные предупреждения продолжают приходить визуально и с вибрацией, но APK выбирает отдельный тихий notification channel без alarm-звука. Обычные уведомления о том, что приём отмечен, и обычные предупреждения остаются без изменений.

Системные настройки notification channels Android имеют приоритет над настройками приложения: пользователь всегда может дополнительно изменить звук/вибрацию в настройках Android.
